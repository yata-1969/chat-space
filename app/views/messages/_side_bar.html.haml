.chat-side
  .chat-side_user-name
    .top-items
      %p.top-items__name
        = current_user.name
      %ul.top-items__icons
        %li.icon-edit
          = link_to new_group_path do
            = icon('far', 'edit', class: "icon")
        %li.icon-cog
          = link_to edit_user_path(current_user) do
            = icon('fa', 'cog', class: "icon")
  .side-bar_group-list
    - current_user.groups.each do |group|
      .side-bar_group
        = link_to '#' do
          .side-bar_group-name
            = group.name
          .side-bar_group-message メッセージはまだありません